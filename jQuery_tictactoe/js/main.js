const View = require('./ttt-view.js');
const Game = require('./game.js');

$( () => {
  // Your code here
  const $container = $("figure.ttt");
  const theView = new View (new Game, $container);








});
